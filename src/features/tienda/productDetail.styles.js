import { BORDERS, COLORS, FONTS } from "@/design/tokens";

export const bd = BORDERS.light;
export const mono = { fontFamily: FONTS.mono };

export const S = {
  bg: COLORS.pageLight,
  text: COLORS.textOnLight,
  muted: COLORS.textMutedLight,
  media: COLORS.pageLight,
  mediaLine: "#d9d9d6",
};
